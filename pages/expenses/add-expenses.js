import Layout from "../../src/ui-components/layout";
import AddExpenses from "src/Pages/Expenses/add-expenses";

const addexpenses =() => {
    return (
        <Layout>
        <AddExpenses />
        </Layout>
    );
}

export default addexpenses;
