import Layout from "../../src/ui-components/layout";
import AddWebsiteData from "src/Pages/Website-data";

const Websitedata =() => {
    return (
        <Layout>
            <AddWebsiteData />
        </Layout>
    );
}

export default Websitedata;
