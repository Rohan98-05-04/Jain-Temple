import EventData from "src/Pages/Event-data";
import Layout from "../../src/ui-components/layout";

const Eventdata =() => {
    return (
        <Layout>
            <EventData />
        </Layout>
    );
}

export default Eventdata;
