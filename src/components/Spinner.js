import React from 'react';
import styles from '../../styles/Spinner.module.css'; // Create a CSS file for styling

const Spinner = () => {
  return <div className={styles.loading} />
};

export default Spinner;
